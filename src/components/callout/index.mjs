import {h} from 'hastscript'

const Callout = (properties, children) => {
    // return h('article', `.${properties.className[0]}`,
    return h('article',    
    children
    )
}


export { Callout }